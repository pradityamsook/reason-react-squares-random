{
  "name": "rects-random-color",
  "version": "0.1.0",
  "scripts": {
    "re:build": "rescript clean && rescript build",
    "build": "bsb -make-world && webpack -w && mv /bundleOutput /build",
    "start": "bsb -make-world -w -ws _ ",
    "clean": "bsb -clean-world",
    "server": "moduleserve ./ --port 8000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "webpack:production": "NODE_ENV=production webpack"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "reason-react": ">=0.9.1",
    "webpack": "^5.74.0",
    "webpack-cli": "^4.10.0"
  },
  "devDependencies": {
    "bs-platform": "^7.1.1",
    "moduleserve": "^0.9.1"
  }
}